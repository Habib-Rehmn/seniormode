# Django Example

SeniorMode should prefer model constraints, forms, built-in validators, and
existing app helpers before adding service layers or new dependencies.

Useful prompt:

```txt
/seniormode full
Fix the root cause and check other callers before editing.
```
