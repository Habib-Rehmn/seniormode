# Next.js Example

SeniorMode should prefer existing route handlers, shared validators, native form
controls, and framework primitives before adding new client state, wrappers, or
dependencies.

Useful prompt:

```txt
/seniormode full
Implement this in the existing feature folder. Reuse current helpers.
```
