description = "Review code for SeniorMode fit"
prompt = "Review this plan, diff, or implementation in SeniorMode review mode. Lead with bugs and risks, then identify overengineering, missed reuse, unnecessary dependencies, unnecessary abstractions, and missing focused tests."
